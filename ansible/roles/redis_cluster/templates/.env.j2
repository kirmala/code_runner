POSTGRES_DB="{{ postgresql_user }}"
POSTGRES_USER="{{ postgresql_database }}"
POSTGRES_PASSWORD="{{ vault_postgresql_password }}"
POSTGRES_HOST="{{ postgresql_host }}"
POSTGRES_PORT="{{ postgresql_port}}"

REDIS_PASSWORD="{{ vault_redis_password }}"
REDIS_ADDRESSES="{{ redis_addresses }}"
