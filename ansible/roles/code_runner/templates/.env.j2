POSTGRES_DB="{{ code_runner.postgres.database }}"
POSTGRES_USER="{{ code_runner.postgres.user }}"
POSTGRES_PASSWORD="{{ vault_postgresql_password }}"
POSTGRES_HOST="{{ code_runner.postgres.host }}"
POSTGRES_PORT="{{ code_runner.postgres.port }}"

REDIS_PASSWORD="{{ vault_redis_password }}"
REDIS_ADDRESSES="{{ code_runner.redis.addresses }}"
